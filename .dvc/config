[core]
    remote = minio
[cache]
    type = copy
['remote "minio"']
    url = s3://data
    endpointurl = http://localhost:9000
    access_key_id = minioadmin
    secret_access_key = minioadmin
['remote "origin"']
    url = https://dagshub.com/HaroonNU2555/mlops-project.dvc
